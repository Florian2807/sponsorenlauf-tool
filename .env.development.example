# Local Node development defaults are created automatically by `npm run dev`.
# Override these only when a custom local location is needed.
SPONSORENLAUF_DATABASE_PATH=.local-data/development.db
SPONSORENLAUF_BACKUP_DIRECTORY=.local-data/backups
